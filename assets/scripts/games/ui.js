'use strict';

const success = (data) => {
  //console.log(data);
};

const failure = (error) => {
  //console.error(error);
};

const createSuccess = (response) => {
  //console.log(response);
};

const updateSuccess = (response) => {
  //console.log(response);
};

module.exports = {
  success,
  failure,
  createSuccess,
  updateSuccess,
};
